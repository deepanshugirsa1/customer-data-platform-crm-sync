services:
  clickhouse:
    image: clickhouse/clickhouse-server:24.3
    ports:
      - "8123:8123"
      - "9000:9000"
    profiles: ["full"]
  api:
    build: .
    ports:
      - "8080:8080"
    command: uvicorn src.api.query_api:app --host 0.0.0.0 --port 8080
    profiles: ["full"]
